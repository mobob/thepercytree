{
  "name": "hashthepercytree",
  "private": true,
  "type": "module",
  "scripts": {
    "data": "bun scripts/ingest-export.ts",
    "dev": "vite",
    "build": "bun scripts/ingest-export.ts && vite build",
    "preview": "vite preview",
    "deploy": "bun run scripts/deploy.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
