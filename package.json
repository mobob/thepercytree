{
  "name": "hashthepercytree",
  "private": true,
  "type": "module",
  "scripts": {
    "data": "bun scripts/ingest-export.ts && bun scripts/thumbs.ts",
    "thumbs": "bun scripts/thumbs.ts",
    "dev": "vite",
    "build": "vite build",
    "build:data": "bun scripts/ingest-export.ts && bun scripts/thumbs.ts && vite build",
    "preview": "vite preview",
    "deploy": "bun run scripts/deploy.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
